# Prueba de conexión a la API de Spotify

Una vez clonado el repositorio, bajar dependencias con:

    $ npm install

## Ejemplo
Para hacer funcionar la aplicación, ve a la carpeta `app`

    $ cd app
    $ node app.js

Luego, abre `http://localhost:8080` en el navegador.
